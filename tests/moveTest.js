var Move = require('../lib/move.js');
var Tile = require('../lib/tile.js');
var Piece = require('../lib/piece.js');

module.exports = {
  setUp: function(callback) {
         callback();
         },
  testMovementExecution: function(test) {
                           test.done();
                         }
};
  
